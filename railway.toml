[deploy]
# Repo-root asgi.py prepends ./backend to sys.path (works without pip install ./backend).
# Port is hardcoded to 8000 — Railpack does not expand $PORT in build-time config.
startCommand = "uvicorn asgi:app --host 0.0.0.0 --port 8000"
