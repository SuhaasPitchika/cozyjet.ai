[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cozyjet-backend"
version = "0.1.0"
description = "CozyJet FastAPI backend"
requires-python = ">=3.12,<3.13"

[tool.setuptools.packages.find]
where = ["."]
