# Install backend deps + register `app` on PYTHONPATH (fixes ModuleNotFoundError: No module named 'app').
[phases.install]
cmds = [
  "pip install -r backend/requirements.txt",
  "pip install ./backend",
]

[start]
cmd = "uvicorn asgi:app --host 0.0.0.0 --port 8000"
